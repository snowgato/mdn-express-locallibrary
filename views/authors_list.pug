extends layout

block content
  h1= title

  ul
    each author in authors_list
      li
        a(href=author.url) #{author.family_name}, #{author.first_name}
        |  (#{author.formatted_bdate} - #{author.formatted_ddate})
    else
      li There are no authors.
